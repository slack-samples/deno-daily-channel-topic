// Included as an example. Schedules will be created programmatically
import { Trigger } from "deno-slack-api/types.ts";

const trigger : Trigger = {
      type: "scheduled",
      name: "daily_schedule",
      workflow: "#/workflows/update_topic",
      schedule: {
        start_time: "2022-08-0T15:30:00Z",
        timezone: "UTC",
        frequency: {
          type: "daily",
          repeats_every: 1
        }
      }
    }

export default trigger;
